#!/usr/bin/env sh
# Placeholder gradlew. GitHub Actions uses gradle/actions/setup-gradle.
# If you build locally later, replace with real gradlew.
echo "Use GitHub Actions to build, or replace this with real gradlew."
exit 0
